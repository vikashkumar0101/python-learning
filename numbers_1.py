# from statistics import mean, median, mode
import numpy as np
# list1 = [1,4,5,6,4,3]
# print(f"The mean of the numbers is:{mean(list1)}")
# print(f"The mean of the numbers is:{median(list1)}")
# print(f"The mean of the numbers is:{mode(list1)}")
arr = np.array([1,2,3,4,5,6])
print(arr)